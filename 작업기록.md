# Golf PT Collabo — 작업 기록 (Project Log)

> 내셔널짐 골프 스튜디오 협업 코칭 플랫폼
> 최종 업데이트: 2026-07-10 / 현재 버전 **v8.8**

---

## 1. 프로젝트 개요

**무엇**: 골프 프로 2명(정우진·홍태양) + PT 트레이너 2명(최현승·이상렬)이
한 회원의 데이터를 공유하며 협업 코칭하는 PWA.

**어디**: `https://wellnessmode.github.io/Golf_PT_collabo/`
(홈화면에 앱으로 추가해서 사용 — 아이폰/아이패드/갤럭시/데스크탑)

**스튜디오 구성**: 3개 베이
- 1번타석 / 2번타석 — 연습·레슨 겸용 (practice 기본)
- 3번룸 — 레슨 전용 (lesson_only, 선별 저장)

**핵심 가치**: 트랙맨 샷 데이터·영상이 자동으로 회원 기록에 쌓이고,
프로/트레이너가 폰만 들고 라이브 수업 → 일지·리포트까지 한 번에.

---

## 2. 시스템 구조

```
[트랙맨 iO PC]                    [클라우드]                  [폰/태블릿 PWA]
 3번룸 (TMB0201)                                              프로·트레이너·관리자
   │ .ftmf 생성                                                    │
   ▼                                                               │
 golfpt-sync 에이전트  ──샷 데이터──▶  Supabase ──────────────▶  앱(라이브)
 (C:\golfpt-sync\)     ──영상(mp4)──▶  Cloudflare R2 ──────────▶  영상 재생
 Node.js, 백그라운드                   Cloudflare Worker
                                       ├ R2 프록시
                                       ├ /claude (AI 일지)
                                       └ /stt (음성→텍스트)
```

### 구성요소

| 요소 | 위치 | 역할 |
|---|---|---|
| **PWA 앱** | GitHub Pages | 회원·세션·라이브·성과·리포트 UI |
| **베이 에이전트** | `C:\golfpt-sync\` (3번룸 PC) | 트랙맨 ftmf 감시→파싱→업로드 |
| **Supabase** | cacytkmijttyeasmhvhq | 회원·세션·샷·활성세션 DB |
| **Cloudflare R2** | golf-pt-storage worker | 영상 저장 + AI/STT 프록시 |

### 주요 파일

```
js/
  app_core.js      상태(S)·저장·클라우드·생체인증·세션복원·렌더 보조
  app_render.js    메인 렌더(스크롤 보존 포함)·역할 선택·비번 모달
  app_handlers.js  회원/세션 CRUD·리포트
  app_ai.js        AI 일지 정리·미디어DB·init()
  app_dashboard.js 성과 리포트·영상 모달·단위 변환
  app_live.js      수업 센터·베이카드·녹음·폴링·샷 선별저장·삭제
agent/
  agent.js         에이전트 본체
  ftmf-parser.js   ftmf(ZIP) 파서 — 트랙맨 메트릭 추출
  config.json      설정(키·베이매핑·ffmpeg 경로) ※ PC에만, git 제외
  설치_자동시작.bat / 상태확인.bat / 중지.bat / 자동시작_해제.bat
  start-hidden.vbs 창 없이 백그라운드 실행 + 자동복구
  3번룸-설치매뉴얼.md
worker/
  golf-pt-storage-worker.js  R2 + /claude + /stt
```

---

## 3. 완료된 기능 (시간순 주요 작업)

### 워커 — 영상 스트리밍(Range) 지원 (2026-07-11) ★배포 필요
- **증상**: 특정 회원 영상이 재생이 느리거나 아이폰에서 재생 안 됨.
- **원인**: R2 워커 GET 핸들러가 Range 요청을 무시하고 항상 전체 파일을 200으로 반환.
  → 브라우저가 전체를 받아야 재생 시작(느림), iOS Safari 는 206 없으면 재생 거부.
- **수정**: `worker/golf-pt-storage-worker.js` GET/HEAD 에 Range 파싱 + 206 Partial
  Content(Accept-Ranges·Content-Range·Content-Length) 응답 추가.
- **⚠️ 이 파일은 GitHub Pages 로 배포 안 됨** — Cloudflare 에 `wrangler deploy` 또는
  대시보드(Workers & Pages → golf-pt-storage → Edit code → 전체 교체 → Deploy)로
  **따로 배포해야** 반영됨.

### v8.8 — 리포트 실작업 + 프라이버시 + B2B (2026-07-10)
- **녹음 원문 프라이버시**: 레슨 녹음 원문이 지도자·회원 화면에 남지 않음.
  관리자 전용 **🎙 녹음 원문 보관함**(마이페이지)에서만 열람 (AI 정리 검증용).
  화면 숨김뿐 아니라 **저장 자체를 분리** — 원문(supplement)은 관리자 기기에만
  내려받고 트레이너·프로 기기의 localStorage 엔 저장 안 됨(loadAll·save 역할 분기).
  트레이너가 녹음·저장하면 원문은 서버로 올라가 관리자만 열람, 본인 기기엔 안 남음.
- **관리자 비밀번호 변경** 칸 추가.
- **성과 리포트 실데이터 완성**: 하드코딩(250m/89타/가짜 예측) 전부 제거 →
  회원 목표 문자열 파싱 + 실측 페이스 기반 산출. 성장 그래프(세션별 캐리 추이),
  탄착군 산점도(+자동 해석), 클럽 갭 차트, 비포/애프터→샷 모달 연결,
  CSV 내보내기, 공유/인쇄 실동작, 기본 단위 미터.
- **공유 리포트(report.html)**: 트랙맨 측정 섹션(베스트샷·추이 차트·클럽표·샷 영상)
  포함. perfMember 무시 버그 수정.
- **업로드 안정성**: R2 업로드 3회 재시도, 실패 시 세션카드 배지+수동 재업로드.
- **B2B 화이트라벨**: config.js 에 BRAND/INSTRUCTORS/BAYS/PASSWORDS 분리.
  벤치마킹·로드맵은 `docs/B2B-상용화-플랜.md` 참조 (GDR·트랙맨·경쟁사 리서치 기반).

### 트랙맨 연동
- **ftmf 파서** — ZIP 무압축 파싱으로 트랙맨 메트릭 전부 추출
  (clubSpeed, ballSpeed, smash, carry, total, launch, spin, clubPath,
  faceAngle, attack, spinAxis, landAngle 등). 단위: 거리 m·속도 m/s.
- **베이 에이전트** — Data 폴더 읽기 전용 감시 → Supabase insert + R2 영상.
  TPS 무간섭·무흔적(파일 삭제/이동/수정 안 함, PC 영상 원본 절대 미삭제).
- **샷 시각 = 처리 시각(now)** — ftmf 내부 타임존 혼선 회피.
- **PC 시계 자가검증** — 시작 시 Google+Cloudflare 교차검증, ±차이 로그.
- **MKV→MP4 자동 변환** — ffmpeg로 변환해 함께 업로드(아이폰 재생).

### 에이전트 운영 자동화
- **원클릭 자동시작** — `설치_자동시작.bat` → 부팅 시 창 없이 백그라운드,
  node 멈춰도 4초 후 자동 재시작.
- **node 절대경로 탐색** — 백그라운드 런처 PATH 문제 해결.
- **config.json BOM 견디기** — 메모장/PowerShell 저장 모두 안전.
- **한글 Windows(CP949) bat 인코딩** — 깨짐 해결.

### 앱 — 수업 센터(구 라이브 세션)
- **🏌️ 수업 센터** 로 개편: [🎯 라이브 수업] / [✏️ 일지만 기록] 분리.
- **레슨/연습 모드** — 3번룸은 선별 저장(좋은 샷만 [＋저장]), 1·2번은 자동.
  모드가 새로고침 후에도 유지(bayMode fallback + applyRemoteActive).
- **방금 친 샷 카드** — 베이카드 상단에 크게, 클럽·캐리·토탈·볼·스핀,
  **미터 표시**, 새 샷 초록 글로우 강조.
- **자동 폴링(4초)** — 새로고침 없이 자동 갱신. 부분 DOM 패치로 화면 안 튐.
- **🔇 샷 수신 일시정지** — 가짜/원치 않는 샷 긴급 차단.

### 앱 — 녹음·일지
- **앱 내장 수업 녹음** — 🎙 버튼 → Whisper(Groq) 글변환 → AI 일지.
  (외부 앱 Alt 불필요, Wake Lock으로 화면 유지)
- **음성 없어도 일지 카드** — 라이브 종료 시 자동으로 일지 작성 모달.

### 앱 — 성과·리포트
- **성과·리포트 통합** — 성과 화면 안에 🤖 리포트 버튼.
- **실데이터만** — mock/데모 샷 영구 제외.
- **영상 모달** — mp4 우선 재생, 풀폭 다운로드 버튼.

### 앱 — 안정성·UX
- **흰 화면 안전망** — render try/catch + 전역 에러 복구 패널.
- **스크롤 보존** — 내부 컨테이너(.live-wrap 등) scrollTop 유지.
- **샷 다중선택·일괄 삭제** — Supabase IN 한방, 즉시 화면 반영(빠릿).
- **한글 검색** — "김"→"ㄱㅣㅁ" 깨짐 + 초성검색 해결(render 미호출 방식).
- **PWA 자동 갱신** — 새 버전 자동 적용(입력/로그인 중엔 보류).
- **Face ID** — 두 번 뜨던 문제 해결(전역 락 + 자동 reload 차단).
- **생체 미지원 기기 자동 로그인** — 구형 아이패드용 '이 기기 자동 로그인'.
- **유령 세션 부활 차단** — 다른 기기 캐시가 삭제 기록 되살리던 문제.
- **전 기기 반응형·텍스트** — keep-all 줄바꿈, iPad 줌인 방지.

---

## 4. 현재 설치 상태 (3번룸 PC)

| 항목 | 상태 |
|---|---|
| Node.js | ✅ 설치됨 |
| 에이전트 파일 | ✅ `C:\golfpt-sync\` (최신) |
| config.json | ✅ 작성됨 (ffmpegPath 포함, BOM 제거됨) |
| ffmpeg | ✅ `C:\ffmpeg\bin\ffmpeg.exe` |
| PC 시계 | ✅ 정확 (±1초) |
| 샷 전송 | ✅ 작동 확인 (웨지 등 정상 수신) |
| MP4 변환 | ✅ 작동 확인 (foreground 실행 시) |
| **자동시작 전환** | ⏳ **미완** — 아래 참조 |

---

## 5. 다음에 이어서 할 일 (남은 작업)

### ① 자동시작 전환 (필수, 5분) ★
> 지금은 PowerShell 창으로 떠 있어 **창 닫으면 멈춤**. 백그라운드 자동으로 전환.

1. PowerShell에서 `Ctrl + C`
2. `설치_자동시작.bat` 더블클릭 (보호창 → 추가정보 → 실행)
3. `상태확인.bat` → **`[ 실행 중 ]`** + **`영상 변환(MP4): ON`** 확인
4. PC 재부팅 → 아무것도 안 하고 `상태확인.bat` → `[ 실행 중 ]` 이면 완료

### ② MP4 영상 재생 최종 확인
- 자동시작 켠 뒤 **새 샷 1발** → 폰 성과 화면 → 그 샷 영상 모달
- 영상 재생 + 다운로드 버튼 안 짤림 확인
- (옛 mkv 샷은 변환 전이라 재생 안 될 수 있음 — 새 샷으로 테스트)

### ③ 토탈 1.6m 차이 정밀 조정 (옵션)
- 증상: 우리 Total 81.7m vs TPS 화면 80m (캐리·나머지는 정확)
- ftmf 안에 `Total` 키 하나뿐이라, TPS가 다른 계산을 쓰는 것으로 추정
- **확인 방법**: `C:\ProgramData\TrackMan\TrackMan Performance Studio\Data`
  의 최근 `.ftmf` 파일 하나를 채팅에 올리면 전체 구조 분석 → 80 만드는
  필드 탐색. 있으면 `ftmf-parser.js`의 total 매핑 한 줄 변경.

### ④ 추가 검토 거리
- 성과·리포트 화면 단위(현재 yd/m 토글) — 기본 미터 고정 원하면 변경
- 1·2번타석 PC 에이전트 설치 (각 PC TrackingUnit 시리얼로 bayMap 추가)
- PT 회원 일지/리포트 워크플로 다듬기

---

## 6. 운영 매뉴얼 (프로·트레이너용)

### 평소 (PC)
PC 켜짐 → 에이전트 자동 실행(창 없음) → **손 댈 일 없음**

### 라이브 수업 (폰)
1. 🏌️ 수업 센터 → 🎯 라이브 수업
2. 회원 선택 → 베이 선택 (3번룸 등)
3. 공 치기 → 4초 내 베이카드 상단에 샷 자동 표시
4. **3번룸(레슨)**: 좋은 샷만 [＋저장], 나머지 [버림]
5. (선택) 🎙 수업 녹음 → 종료 시 AI 일지 자동 정리
6. ⏹ 종료 → 세션 일지 카드 작성

### 일지만 기록 (샷 없이)
🏌️ 수업 센터 → ✏️ 일지만 기록 → 회원 선택 → 바로 작성

### 관리 도구 (PC, C:\golfpt-sync\)
| 파일 | 용도 |
|---|---|
| 설치_자동시작.bat | 자동시작 등록 + 지금 실행 |
| 상태확인.bat | 실행여부 + PC시계 + 최근 로그 |
| 중지.bat | 지금 중지 |
| 자동시작_해제.bat | 자동시작 해제 + 중지 |

---

## 7. 문제 해결 (자주 묻는 것)

| 증상 | 해결 |
|---|---|
| 공 쳤는데 폰에 안 뜸 | ① 🔇 수신정지 풀었나 ② 상태확인 [실행중] ③ 회원 배정했나 |
| 상태확인 [중지됨] | 설치_자동시작.bat 다시 실행 |
| 영상 검은 화면 | 그 샷이 mp4 변환됐나(상태확인 로그 'MP4 변환·업로드') / 옛 mkv 샷은 다운로드로 |
| 가짜 샷 폭주 | 폰 수업센터 → 🔇 샷 수신 일시정지 |
| Face ID 두 번 | v7.8 이후 해결 — 앱 완전종료 후 재실행으로 갱신 |
| 화면 위로 튐 | v7.8 이후 해결 — 앱 갱신 |
| config.json 읽기 실패 | BOM 문제 — 에이전트가 v64e8b2d 이후 자동 처리 |
| bat 글자 깨짐 | 우클릭 → 연결 프로그램 → Windows 명령 처리기 |

문제 시 `C:\golfpt-sync\agent.log` 마지막 부분 공유.

---

## 8. 설정·키 참조

### config.json (3번룸 PC)
```json
{
  "SUPABASE_URL": "https://cacytkmijttyeasmhvhq.supabase.co",
  "SUPABASE_ANON_KEY": "(공개 anon key — 앱 config.js와 동일)",
  "R2_WORKER_URL": "https://golf-pt-storage.ceo-fc9.workers.dev",
  "R2_API_KEY": "(앱 R2 키와 동일)",
  "watchDirs": ["C:\\ProgramData\\TrackMan\\TrackMan Performance Studio\\Data"],
  "bayMap": { "24240089": "bay3" },
  "defaultBay": "bay3",
  "uploadVideo": true,
  "ffmpegPath": "C:\\ffmpeg\\bin\\ffmpeg.exe",
  "intervalSec": 5,
  "processExisting": false,
  "backfillMinutes": 0
}
```

### Cloudflare Worker 시크릿 (golf-pt-storage)
- `APP_API_KEY` — R2 인증
- `ANTHROPIC_API_KEY` — AI 일지 정리 (sk-ant-...)
- `GROQ_API_KEY` — 음성→텍스트 Whisper (gsk_...)

### 로그인
- 관리자 비밀번호: `admin0000`
- 트랙맨 3번룸 시리얼(TrackingUnit): `24240089`

---

## 9. 변하지 않는 원칙 (중요)

1. **PC의 트랙맨 영상 원본은 어떤 경우에도 삭제하지 않는다.**
   (앱 전체삭제·미배정삭제·선택삭제 모두 DB+R2 사본만, PC 원본 무관)
2. **에이전트는 트랙맨 파일을 읽기 전용으로만** 본다 (삭제·이동·수정 X).
3. **트랙맨 코리아 원격 점검 시 TPS는 평소와 동일하게 보인다** (무간섭·무흔적).
4. 모든 개발은 `claude/continue-session-fix-errors-t8Dsm` 브랜치 → main 머지.

---

## 10. 커밋 히스토리 (주요)

최신순. 전체는 `git log` 참조.

```
85d8dec fix(영상): mp4 키 경로 + 다운로드 버튼 짤림
e3f5f24 ui(라이브): 수업센터 거리 표시를 미터로
64e8b2d fix(agent): config.json BOM 견디기 + 영상변환 ON/OFF 로그
b3a66c3 diag: 토탈 후보 키들 진단
659b0b2 feat: MKV→MP4 변환 + 영상 다운로드 버튼
7d898a8 fix+perf: 선택모드 스크롤 안 튐 + 샷 삭제 빠릿빠릿
6aea9bc fix(스크롤): 내부 컨테이너 scrollTop 보존
96f1d7e fix(스크롤): render() 자체에 스크롤 자동 보존
ee33abc fix(라이브): 부분 DOM 패치 — 화면 고정 갱신
34f7eff fix: Face ID 잔여 시트 + 폴링 스크롤 튐
04e2c16 feat(라이브): 자동 갱신(4초) + 샷 카드 상단·강조
95e0883 fix(로그인): Face ID 두 개 동시 + 자동 reload 차단
3744211 fix(레슨모드): 새로고침 후 자동저장 버그
7490b28 fix(agent): 자동시작 node 절대경로
cdd2480 fix: 한글검색 깨짐 근본해결 + PWA 자동 업데이트
455c516 feat(녹음): 앱 내장 수업 녹음 → Whisper → AI 일지
93b9339 fix(세션부활): 삭제 기록 되살아남 + 유령 정리
1b37333 fix(샷차단): mock 무시 + 🔇 수신 일시정지
ea92369 feat(수업센터): 라이브 세션 개편 + 일지만 기록 분리
9b70848 feat(agent): 원클릭 자동시작
121b014 feat(agent): PC 시계 자가검증
1b603fc feat(agent): TrackMan iO 실데이터 연동
```

---

*이 문서는 작업 재개용 요약입니다. 다음에 "자동시작 전환부터" 또는
"토탈 ftmf 분석부터" 처럼 시작점을 말씀하시면 바로 이어서 진행합니다.*
